/*

Lưu ý: 
- khi nộp bài: Copy toàn bộ lệnh SQL vào file .txt. Và nộp file này nhớ ghi lại đề nhé
- chạy 1 lần là được không bị lỗi.
- Bấm chạy lại lần nữa cũng không được lỗi(học cách drop if exists)

*/
-- Create database
USE MASTER
GO
DROP DATABASE IF EXISTS LECTURE6_HOMEGIFT
GO
CREATE DATABASE LECTURE6_HOMEGIFT
GO

USE LECTURE6_HOMEGIFT;
GO

-- Create tables
CREATE TABLE Products (
    ID INT PRIMARY KEY,
    Name VARCHAR(50) NOT NULL,
    Price DECIMAL(10, 2) NOT NULL
);

CREATE TABLE Orders (
    ID INT PRIMARY KEY,
    CustomerName VARCHAR(50) NOT NULL,
    OrderDate DATE NOT NULL,
    TotalPrice DECIMAL(10, 2) NOT NULL
);

CREATE TABLE OrderItems (
    OrderID INT FOREIGN KEY REFERENCES Orders(ID),
    ProductID INT FOREIGN KEY REFERENCES Products(ID),
    Quantity INT NOT NULL DEFAULT 1,
    CONSTRAINT PK_OrderID_ProductID PRIMARY KEY (OrderID, ProductID)    
);

-- Insert data
INSERT INTO Products (ID, Name, Price) VALUES
(1, 'Product A', 100.00),
(2, 'Product B', 200.00),
(3, 'Product C', 300.00),
(4, 'Product D', 400.00),
(5, 'Product E', 500.00);

INSERT INTO Orders (ID, CustomerName, OrderDate, TotalPrice) VALUES
(1, 'Customer A', '2022-01-01', 800.00),
(2, 'Customer B', '2022-01-02', 1000.00),
(3, 'Customer C', '2022-01-03', 1200.00),
(4, 'Customer D', '2022-01-04', 1500.00),
(5, 'Customer E', '2022-01-05', 1800.00);

INSERT INTO OrderItems (OrderID, ProductID, Quantity) VALUES
(1, 1, 2),
(1, 2, 1),
(1, 3, 3),
(2, 1, 1),
(2, 2, 2),
(2, 3, 4),
(2, 4, 1),
(3, 2, 1),
(3, 4, 2),
(4, 1, 3),
(4, 2, 2),
(4, 3, 4),
(4, 4, 1),
(5, 1, 2),
(5, 3, 3),
(5, 5, 1);
-- Note: You can use any form of subquery (correlated or non-correlated) to solve these problems.
--Find the names and prices of products that have been ordered at least once.

--Find the names and total prices of orders that include at least one product with a price greater than $400.

--Find the names of customers who have placed an order containing at least one product with a price greater than $400.

--Find the IDs and names of products that have not been ordered.

--Find the total number of products ordered for each order, sorted in descending order by the number of products.

--Find the names of products that have been ordered by all customers.

--Find the IDs and names of products that have been ordered by exactly two different customers.
